@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model Tuple<IEnumerable<VLUTUTORS.Models.Taikhoannguoidung>, IEnumerable<VLUTUTORS.Models.Taikhoannguoidung>>
@{
    ViewData["Title"] = "Chi tiết thống kê";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href='javascript:history.go(-1)'>Trang chủ</a></li>
                    <li class="breadcrumb-item active">Chi tiết thống kê người dùng</li>
                </ol>
            </div>
        </div>
        <div class="container-fluid">
            <div class="row mb-2 justify-content-center text-center text-uppercase">
                <div class="col-sm-6">
                    <h1>Danh Sách Người Dùng</h1>
                </div>
            </div>
        </div>
        <!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">
        <!-- Default box -->
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Tài khoản mới trong 1 tuần qua:</h3>
            </div>
            <div class="card-body p-0">
                <table class="table table-striped projects" id="myTable">
                    <thead>
                        <tr>
                            <th class="text-center">#</th>
                            <th class="text-center">Họ tên</th>
                            <th class="text-center">Email</th>
                            <th class="text-center">Tài khoản</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            var index = 0;
                            foreach (var item in Model.Item1)
                            {
                                index++;
                                <tr>
                                    <td class="text-center">
                                        @index
                                    </td>
                                    <td class="text-center">
                                        @Html.DisplayFor(modelItem => item.HoTen)
                                    </td>
                                    <td class="text-center">
                                        @Html.DisplayFor(modelItem => item.Email)
                                    </td>
                                    @{
                                        if (item.IdxetDuyet == 5)
                                        {
                                            <td class="text-center">
                                                Gia sư
                                            </td>
                                        }
                                        else
                                        {
                                            <td class="text-center">
                                                Người học
                                            </td>
                                        }
                                    }
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
            <!-- /.card-body -->
        </div>
        <div class="card">
            <div class="card-header">
                <div class="card-tools">
                </div>
            </div>
            <div class="card-body p-0">
                <table class="table table-striped projects" id="myTable2">
                    <thead>
                        <tr>
                            <th class="text-center">#</th>
                            <th class="text-center">Họ tên</th>
                            <th class="text-center">Email</th>
                            <th class="text-center">Tài khoản</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            var index2 = 0;
                            foreach (var item in Model.Item2)
                            {
                                index2++;
                                <tr>
                                    <td class="text-center">
                                        @index
                                    </td>
                                    <td class="text-center">
                                        @Html.DisplayFor(modelItem => item.HoTen)
                                    </td>
                                    <td class="text-center">
                                        @Html.DisplayFor(modelItem => item.Email)
                                    </td>
                                    @{
                                        if (item.IdxetDuyet == 5)
                                        {
                                            <td class="text-center">
                                                Gia sư
                                            </td>
                                        }
                                        else
                                        {
                                            <td class="text-center">
                                                Người học
                                            </td>
                                        }
                                    }
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->
    </section>
    <!-- /.content -->
</div>

